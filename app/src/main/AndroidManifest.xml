<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.b3espi.gamelib">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuizzPlay" />
        <activity android:name=".QuizzStats" />
        <activity android:name=".DemineurPlay" />
        <activity android:name=".DemineurStats" />
        <activity android:name=".Puissance4Play" />
        <activity android:name=".Puissance4Stats" />
        <activity android:name=".Quarto"/>
    </application>

</manifest>